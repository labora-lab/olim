[project]
name = "olim"
version = "0.1.0"
description = "Open Labeller for Interactive Machine Learning"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary]>=3.3.4",
    "sqlalchemy>=2.0.51",
]

[dependency-groups]
dev = [
    "prek>=0.4.5",
    "ruff>=0.15.20",
    "ty>=0.0.55",
]
